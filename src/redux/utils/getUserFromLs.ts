/*import {usersSliceType} from '../userInfo/slice'

type userLS = {
    email:string,
    id:string,
    token:string
}

export const getUserFromLS = () => {
    const data = localStorage.getItem('user')
    //@ts-ignore
    const user = JSON.parse(data);


        return {
            user,
            
        }

}*/